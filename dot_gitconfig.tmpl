[user]
  name = Michael Wagner
  email = {{ .email }}
;[core]
  ;editor = nvim
;[difftool]
  ;prompt = true
;[diff]
  ;tool = nvimdiff
;[difftool "nvimdiff"]
  ;cmd = "nvim -d\"$LOCAL\" \"$REMOTE\""
[credential]
  helper = "cache --timeout=14400" ;4 hours
  ;helper = !git-credential-1password ;TODO: better alternative
  ;helper = store ;TODO: could be used for $profile=client
[alias]
  slog = log --graph --all --topo-order --pretty='format:%h %ai %s%d (%an)'
